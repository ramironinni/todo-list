import { localStorageUpdateSubtaskChecked } from "../../../../../db/update/update-subtask-checked";

export function toggleSubtaskChecked(e) {
    console.dir(e.target);
    // const taskId = e.target.parentNode.parentNode.id;
    // localStorageUpdateSubtaskChecked(taskId);
}
